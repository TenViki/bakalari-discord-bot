import axios from "axios";
import config from "config";
import { Subject, Timetable } from "../types/Timetable.type";
import { JSDOM } from "jsdom";

export const getTimetable = (): void => {
  // TODO: Fetch table
};
